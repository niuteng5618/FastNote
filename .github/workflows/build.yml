name: Build Multi-Platform

on:
  workflow_dispatch:
    inputs:
      linux:
        description: '同时编译 Linux'
        type: boolean
        default: false
      macos:
        description: '同时编译 macOS'
        type: boolean
        default: false
  push:
    tags:
      - 'v*'

jobs:
  build:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-2022
            os: Windows
            arch: x64
          - platform: ubuntu-22.04
            os: Linux
            arch: x64
          - platform: macos-14
            os: macOS
            arch: universal

    runs-on: ${{ matrix.platform }}
    # 注：不能在 job 级 if 引用 matrix 上下文（GitHub 在 job 启动前评估 if，此时 matrix 不可用，
    # 会导致整个 workflow 解析失败）。改用 step 级 if 控制非 Windows 平台仅在 workflow_dispatch 时构建，
    # tag push 只编译 Windows（符合「仅编译 win」需求）。

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ startsWith(matrix.platform, 'macos-') && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      # Windows 特定依赖
      - name: Install Windows bundlers (NSIS + WiX)
        if: matrix.os == 'Windows'
        shell: pwsh
        run: |
          choco install nsis -y
          choco install wixtoolset -y

      - name: Install Windows build prerequisites (CMake + NASM)
        if: matrix.os == 'Windows'
        shell: pwsh
        run: |
          choco install cmake --installargs 'ADD_CMAKE_TO_PATH=System' -y
          choco install nasm -y

      # Linux 特定依赖（仅手动触发且勾选 Linux 时运行；tag push 不编译 Linux）
      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04' && github.event_name == 'workflow_dispatch' && inputs.linux
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            build-essential \
            curl \
            wget \
            file \
            patchelf \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Install Tauri CLI
        run: npm i -D @tauri-apps/cli@^2

      - name: Install frontend dependencies
        run: npm ci

      - name: Generate icons from Flymdnew.png
        run: npx tauri icon ./Flymdnew.png
      - name: Install macOS DMG toolchain
        if: startsWith(matrix.platform, 'macos-') && github.event_name == 'workflow_dispatch' && inputs.macos
        run: |
          brew update || true
          brew list create-dmg >/dev/null 2>&1 || brew install create-dmg
          xcrun --find SetFile >/dev/null 2>&1 || sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer || true
      - name: Build .app (macOS Universal)
        if: startsWith(matrix.platform, 'macos-') && github.event_name == 'workflow_dispatch' && inputs.macos
        env:
          RUST_LOG: trace
          TAURI_BUNDLER_TARGET: universal-apple-darwin
        run: npm run tauri:build -- --bundles app --target universal-apple-darwin --verbose
      - name: Zip .app for convenience (macOS)
        if: startsWith(matrix.platform, 'macos-') && github.event_name == 'workflow_dispatch' && inputs.macos
        run: |
          APP_DIR="src-tauri/target/universal-apple-darwin/release/bundle/macos/FastNote.app"
          OUT_DIR="src-tauri/target/universal-apple-darwin/release/bundle/macos"
          if [ -d "$APP_DIR" ]; then
            (cd "$OUT_DIR" && zip -qry FastNote_${{ matrix.arch }}.app.zip FastNote.app)
          fi
      - name: Build DMG (macOS Universal, with fallback)
        if: startsWith(matrix.platform, 'macos-') && github.event_name == 'workflow_dispatch' && inputs.macos
        env:
          RUST_LOG: trace
          TAURI_BUNDLER_TARGET: universal-apple-darwin
        run: |
          set -e
          APP_DIR="src-tauri/target/universal-apple-darwin/release/bundle/macos/FastNote.app"
          DMG_DIR="src-tauri/target/universal-apple-darwin/release/bundle/dmg"
          mkdir -p "$DMG_DIR"
          echo "== Try tauri bundler (dmg) =="
          if npm run tauri:build -- --bundles dmg --target universal-apple-darwin --verbose; then
            echo "Tauri DMG build OK"
          else
            echo "::warning::Tauri DMG failed. Fallback to create-dmg"
            brew list create-dmg >/dev/null 2>&1 || brew install create-dmg
            OUT_DMG="$DMG_DIR/FastNote_${{ matrix.arch }}.dmg"
            if [ ! -d "$APP_DIR" ]; then
              echo "::error::App not found for DMG fallback: $APP_DIR"
              exit 1
            fi
            rm -f "$OUT_DMG"
            create-dmg --dmg-title "FastNote" --volname "FastNote" --icon-size 128 \
              --app-drop-link 480 180 --icon "FastNote.app" 160 180 \
              "$OUT_DMG" "$APP_DIR" || { echo "::error::Fallback create-dmg failed"; ls -la "$DMG_DIR"; exit 1; }
          fi
      - name: Upload DMG workdir on failure
        if: failure() && startsWith(matrix.platform, 'macos-') && github.event_name == 'workflow_dispatch' && inputs.macos
        uses: actions/upload-artifact@v4
        with:
          name: dmg-workdir-${{ matrix.arch }}
          path: src-tauri/target/universal-apple-darwin/release/bundle/dmg/

      - name: Build Tauri app (Windows)
        if: matrix.os == 'Windows'
        run: npm run tauri:build -- --bundles nsis
      - name: Build Tauri app (Linux)
        if: matrix.platform == 'ubuntu-22.04' && github.event_name == 'workflow_dispatch' && inputs.linux
        run: npm run tauri:build -- --bundles deb appimage

      - name: Create portable zip (Windows)
        if: matrix.os == 'Windows'
        shell: pwsh
        run: |
          # 注：Tauri 二进制名取自 Cargo.toml 的 [package] name（内部标识符 com.flymd 的衍生，
          # 不能改），产物为 flymd.exe；productName 只影响安装包/窗口标题等用户可见处。
          $exe = "src-tauri/target/release/flymd.exe"
          if (!(Test-Path $exe)) { throw "portable: exe not found: $exe" }
          $outDir = "src-tauri/target/release/bundle/portable"
          New-Item -ItemType Directory -Force -Path $outDir | Out-Null
          $zip = Join-Path $outDir "FastNote_portable_${{ matrix.arch }}.zip"
          if (Test-Path $zip) { Remove-Item $zip -Force }
          Compress-Archive -Path $exe -DestinationPath $zip -Force

      - name: Upload artifacts (Windows Portable)
        if: matrix.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: FastNote-${{ matrix.os }}-${{ matrix.arch }}-portable
          path: src-tauri/target/release/bundle/portable/*.zip
          if-no-files-found: error

      - name: Upload artifacts (Windows)
        if: matrix.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: FastNote-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error

      - name: Upload artifacts (Linux)
        if: matrix.platform == 'ubuntu-22.04' && github.event_name == 'workflow_dispatch' && inputs.linux
        uses: actions/upload-artifact@v4
        with:
          name: FastNote-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/appimage/*.AppImage
          if-no-files-found: warn

      - name: Upload artifacts (macOS)
        if: startsWith(matrix.platform, 'macos-') && github.event_name == 'workflow_dispatch' && inputs.macos
        uses: actions/upload-artifact@v4
        with:
          name: FastNote-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg
            src-tauri/target/universal-apple-darwin/release/bundle/macos/*.app.zip
          if-no-files-found: warn

  # 可选：创建 Release
  create-release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            FastNote-Windows-x64/**/*
            FastNote-Windows-x64-portable/**/*
            FastNote-Linux-x64/**/*
            FastNote-macOS-universal/**/*
          draft: true
          generate_release_notes: true






